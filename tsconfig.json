{
  "compilerOptions": {
    "outDir": "./dist/", //куда производится сборка не очень важен так как сборка с помощью вебпак
    "noImplicitAny": true, //не позволяет использловать переменные без типа
    "module": "ESNext", //
    "target": "es5", // версия в которую будет компиляция
    "jsx": "react-jsx",
    "allowJs": true, //позволяет писать js
    "moduleResolution": "node",
    // "baseUrl": ".", //для абсолютного импорта
    "esModuleInterop": true, // позволяет работать с пакетами , которые используют common js как с обычными пкатами с помощью import (require() module.export) = common js
    "allowSyntheticDefaultImports": true // позволяет использовать импорты без * так как изначально пакеты нод не поддеживают экспорт дефолт
  },
  "ts-node": {
    //добавили настройки чтобы сборка выполнилась с импортами
    "compilerOptions": {
      "module": "CommonJS"
    }
  }
}
